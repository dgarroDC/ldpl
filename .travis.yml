os: linux
dist: xenial

language: cpp
compiler: gcc

env: CXX=g++-5 CC=gcc-5 PATH=$PATH:${TRAVIS_BUILD_DIR}/src/

install:
  - git clone https://github.com/Lartu/ldpltest

script:
  - cd src && make
  - cd ../ldpltest && ./compileAndRunTester.sh | grep -c "ALL TESTS PASSED SUCCESSFULLY"
